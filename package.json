{
  "private": true,
  "name": "preact-cps",
  "homepage": "http://man-exists.github.io/preact-cps",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "deploy-to-gh": "preact build && gh-pages -d build",
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^7.17.0",
    "eslint-config-preact": "^1.1.3",
    "gh-pages": "^3.2.3",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2"
  }
}
